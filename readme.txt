Install the libraries in your local machine

1. Install python or anaconda software in your machine
2. Open command prompt and paste this command to install the libraries one by one
```
pip install ctransformers[cuda]
pip install Flask
```
3. Run the app.py in visual studio code
4. update the necessary variables and run test.py